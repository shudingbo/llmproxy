# 跳过所有 lifecycle scripts（包括 node-gyp rebuild）：
# better-sqlite3 v13.0.2 自带 prebuilds（package/prebuilds/*.node），
# lib/binding.js 在 require 时直接加载，无需 install script 触发编译。
# 此配置同时兼容 npm 与 pnpm，避免 Node 22+ 上 npm publish 时隐式注入
# `"install": "node-gyp rebuild"` 后触发 find VS 失败的问题。
# 参考：https://github.com/WiseLibs/better-sqlite3/issues/1503
ignore-scripts=true