{
  "name": "llmproxy",
  "main": "scripts/start.js",
  "private": true,
  "version": "0.6.0",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.7.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -k -n server,web \"pnpm --filter @llmproxy/server dev\" \"pnpm --filter @llmproxy/web dev\"",
    "dev:server": "pnpm --filter @llmproxy/server dev",
    "dev:web": "pnpm --filter @llmproxy/web dev",
    "start": "node scripts/start.js",
    "start:rebuild": "node scripts/start.js --rebuild",
    "build": "pnpm --filter @llmproxy/web build && pnpm --filter @llmproxy/server build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
